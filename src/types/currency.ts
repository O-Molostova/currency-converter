export type Currency = {
  id: number;
  code: string;
  name: string;
  short_code: string;
  symbol: string;
  symbol_first: boolean;
  subunit: string;
  precision: number;
  decimal_mark: string;
  thousands_separator: string;
};
